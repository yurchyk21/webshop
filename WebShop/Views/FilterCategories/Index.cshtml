@model IEnumerable<WebShop.Models.Entities.FilterCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    <p></p>
    @Html.ActionLink("Create New Name", "CreateFilterName")
<p></p>
    @Html.ActionLink("Create New Group", "CreateFilterValue")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategoryOf.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FilterNameOf.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FilterValueOf.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CategoryOf.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FilterNameOf.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FilterValueOf.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
